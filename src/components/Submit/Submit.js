import React, { Component } from 'react';
import style from './Submit.less';

const Submit: Function = (): Object => {
  return (
    <div className={style.search}>
      Hello Submit Component :)
    </div>
  );
};

export default Submit;